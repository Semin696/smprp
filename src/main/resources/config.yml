# ═══════════════════════════════════════════════════════════
#   SDS — Главный конфиг
#   Все настройки модулей вынесены в отдельные файлы:
#
#   autorestart_config.yml    — авторестарт сервера
#   spawn_config.yml          — точка спавна
#   pvp_config.yml            — PvP режим
#   order_config.yml          — система ордеров
#   moneys_config.yml         — экономика
#   playerheaddrop_config.yml — выпадение голов
#   team_config.yml           — команды/кланы
#   tpa_config.yml            — телепортация
#   homesev_config.yml        — дома игроков
#   chat_config.yml           — форматирование чата
# ═══════════════════════════════════════════════════════════
